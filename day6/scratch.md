```
mjqjpqmgbljsphdztnvjfqwrcgsmlb
```

```
add char to the buffer

if length is 4 it's time to check for equality.

  does the hash contain the char?
  
    if so, then 
      don't add the char to the hash
      
  
  if it doesn't contain the char
    add the char to the hash
    
  if hash is size four then caputer the iterator.

 
```